/*
3) Functions:
Description: Functions in TypeScript can have typed parameters and return values, 
enhancing code readability and error checking. 
TypeScript allows you to specify parameter types and the return type. 

// TypeScript Code
function add(a: number, b: number): number {
  return a + b; // Function with typed parameters and a typed return value
}
const result: number = add(3, 4); // Result is type-checked

*/

/*
Type Inference:
Description: TypeScript can infer types when you declare variables. If you don't explicitly specify types, TypeScript automatically assigns types based on the values you initialize variables with, 
reducing the need for redundant type annotations.

// TypeScript Code
let autoType = 42; // TypeScript infers the 'number' type
let alsoAutoType = "Hello, Type Inference!"; // TypeScript infers the 'string' type

 */
